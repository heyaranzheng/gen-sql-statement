# This pre-macro is used to create a specific sql when we have a structure.

This pre-macro have a lot of problems, it's not working properly.
1. can't handle the generic type and lifetime parameters
2. can't handle the "insert" situation